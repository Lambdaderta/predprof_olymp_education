services:
  tts-service:
    build: .
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
    volumes:
      - .:/app
    networks:
      - tts-network

networks:
  tts-network:
    driver: bridge