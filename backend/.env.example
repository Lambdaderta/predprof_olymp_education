# .env
# База данных
DB_HOST=localhost
DB_PORT=5432
DB_NAME=name
DB_USER=user
DB_PASSWORD=your_strong_db_password

# Безопасность
JWT_SECRET_KEY=your_super_strong_secret_key_here_512_bits  # 👈 ОБЯЗАТЕЛЬНО УКАЖИТЕ СВОЙ!
# Генерация 512-битного ключа (64 байта)
# python -c 'import secrets; print(secrets.token_urlsafe(64))'
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30
JWT_REFRESH_TOKEN_EXPIRE_DAYS=7

# MINIO (S3 совместимый)
MINIO_ENDPOINT=localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_SECURE=false
MINIO_BUCKET_NAME=name

# AI сервис
AI_API_BASE=http://localhost:8000/v1
AI_DEFAULT_MODEL=local-model
AI_TIMEOUT=60

# Другие настройки
DEBUG=true