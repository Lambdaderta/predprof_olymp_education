# PREDPROF Education Platform — Документация

## Ссылка на видео проекта
- https://vkvideo.ru/video-235881469_456239018

## Запуск и начальная настройка проекта

### Полный запуск проекта

Весь проект запускается через единый docker compose:

```Bash
docker-compose up -d --build
```

Перед запуском необходимо перейти в ml/llm и скачать модели, или же поменять код так, для обращения к API моделей, а не локальным. Гайд как это сделать будет ниже.

После этого проект будет доступен по адресу http://localhost:3000


#### 3. Запуск компонента парсинга задач

В директории `ml/task_parsing` находится:
- Гайд по настройке и использованию парсера задач
- Примеры задач для обработки
- Скрипты для автоматического парсинга и загрузки в БД

```bash
cd /ml/task_parsing
```

## Описание реализованных функций

В проекте реализовано:

- Аутентификация и авторизация пользователей — регистрация, вход, управление профилем
- Административная панель — интерфейс для управления пользователями, задачами и статистиками
- Работа с задачами — создание, редактирование и управление задачами через админ-панель
- Обработка естественного языка с использованием LLM — интеграция с языковыми моделями для анализа и генерации текста
- Асинхронная обработка — использование очередей сообщений для фоновой обработки задач
- API интерфейсы — RESTful API для взаимодействия между компонентами системы
- Работа с векторными данными — использование pgvector для хранения и поиска векторных представлений
- PVP режим — соревновательный режим между пользователями с рейтинговой системой и статистикой
- Парсинг задач — автоматический анализ и обработка задач с использованием ML. Подробный гайд находится в директории `ml/task_parsing`
- Управление пользователями — назначение ролей, включая права администратора

## Как использовать внешние API
---

### 📝 Минимальные изменения для поддержки внешних API

#### 1️ Обновите `.env` бэкенда (`./backend/.env`)
```env
# Для локального сервера (как сейчас):
AI__LLM_SERVER_URL=http://llm-server:8080
AI__AI_DEFAULT_MODEL=Qwen3-4B-Instruct-2507-Q4_K_M
AI__LLM_API_KEY=  # оставить пустым

# Для OpenAI (пример):
AI__LLM_SERVER_URL=https://api.openai.com
AI__AI_DEFAULT_MODEL=gpt-4o-mini
AI__LLM_API_KEY=sk-proj-ваш_ключ_здесь

# Для Groq:
AI__LLM_SERVER_URL=https://api.groq.com/openai
AI__AI_DEFAULT_MODEL=llama-3.1-70b-versatile
AI__LLM_API_KEY=gsk_ваш_ключ_здесь
```

#### 2️ Добавьте поле в конфиг (`app/core/config.py`)
В класс `AIConfig` добавьте:
```python
from typing import Optional  # убедитесь, что импортировано

class AIConfig(BaseModel):
    # ... существующие поля ...
    LLM_API_KEY: Optional[str] = Field(None, description="API key for external LLM services")
```

#### 3️ Обновите запрос в `courses.py` (функция `generate_similar_task`)
Замените блок отправки запроса:
```python
headers = {"Content-Type": "application/json"}
if settings.ai.LLM_API_KEY:
    headers["Authorization"] = f"Bearer {settings.ai.LLM_API_KEY}"

async with httpx.AsyncClient(timeout=300.0) as client:
    response = await client.post(
        f"{settings.ai.llm_server_url}/v1/chat/completions",
        json={
            "model": settings.ai.AI_DEFAULT_MODEL,  
            "messages": [{"role": "user", "content": prompt}],
            "stream": False
        },
        headers=headers  
    )
```


---

### 🔄 Переключение между режимами
1. Измените значения в `.env`
2. Перезапустите бэкенд:
   ```bash
   docker-compose up -d --build backend
   ```
---
